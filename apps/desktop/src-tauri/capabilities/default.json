{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the Kodo main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
